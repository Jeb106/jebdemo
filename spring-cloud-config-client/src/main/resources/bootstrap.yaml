server:
  port: 8989

spring:
  application:
    name: config-client
  cloud:
    config:
      uri: http://localhost:8889/
      profile: dev
      label: master
management:
  security:
    enabled: false